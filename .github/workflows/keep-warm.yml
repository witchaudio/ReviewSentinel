name: Keep API warm

on:
  schedule:
    - cron: "*/14 * * * *"  # every 14 minutes (Render spins down after 15)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /health
        run: curl -sf https://reviewsentinel-api.onrender.com/health
